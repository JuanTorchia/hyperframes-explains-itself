# Dev.to Package

> Status: publication adaptation. Use canonical links to the juanchi.dev posts after they exist.

## Spanish Version

Title:

```text
HyperFrames explicandose a si mismo: como arme un video tecnico reproducible desde HTML
```

Tags:

```text
webdev, javascript, video, tooling
```

Canonical URL:

```text
https://juanchi.dev/es/blog/hyperframes-video-tecnico-reproducible-html
```

Opening:

```markdown
Queria probar HyperFrames sin quedarme en una conclusion teorica. Asi que hice una prueba bastante directa: usar HyperFrames para construir un video sobre HyperFrames y dejar abierto todo el proceso.

No solo el MP4 final. Tambien el repo, los comandos, los errores, las capturas, el audio, los captions, los fixes y las limitaciones.
```

Recommended body:

Use `docs/article-howto-es.md`, but trim the middle if Dev.to feels too long. Keep these sections:

```text
La pregunta
Que termine construyendo
Paso 1: armar un repo que cuente la historia
Paso 4: elegir Docker como camino principal
Paso 7: probar captions automaticos y captions curados
Paso 8: crear experimentos chicos para no inflar claims
Paso 9: documentar cuando algo no prueba lo que parecia
Mi conclusion
```

Close:

```markdown
El repo publico queda aca:

https://github.com/JuanTorchia/hyperframes-explains-itself

La parte mas interesante para mi no fue el MP4. Fue poder reconstruir el camino completo.

HTML es el source. MP4 es el artefacto.
```

## English Version

Title:

```text
HyperFrames Explains Itself: Building a Reproducible Technical Video From HTML
```

Tags:

```text
webdev, javascript, video, tooling
```

Canonical URL:

```text
https://juanchi.dev/en/blog/hyperframes-reproducible-technical-video-html
```

Opening:

```markdown
I did not want to evaluate HyperFrames by only reading the docs and rendering a tiny sample. So I made the test concrete: use HyperFrames to build a video about HyperFrames, then publish the whole process.

Not just the final MP4. The repository includes commands, mistakes, fixes, screenshots, audio, captions, renders, and evidence.
```

Recommended body:

Use `docs/article-howto-en.md`, but trim the middle if Dev.to feels too long. Keep these sections:

```text
The Question
What I Built
Step 1: Build a Repository That Tells the Story
Step 4: Choose Docker as the Main Render Path
Step 7: Test Automatic Captions and Curated Captions
Step 8: Use Small Experiments Instead of Big Claims
Step 9: Document What the Evidence Does Not Prove
My Takeaway
```

Close:

```markdown
The public repository is here:

https://github.com/JuanTorchia/hyperframes-explains-itself

The most interesting artifact was not only the MP4. It was being able to reconstruct the whole path.

HTML is the source. MP4 is the artifact.
```

